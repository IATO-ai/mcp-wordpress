# Files excluded from the WordPress.org distribution ZIP.
# Used by `wp dist-archive` and similar tools.
.git
.gitignore
.distignore
.DS_Store
.claude/
CLAUDE.md
docs/
*.docx
IATO-Phase2-WordPress.md

# WP.org asset overrides (served from SVN, not in plugin ZIP)
assets/icon-*.png
assets/screenshot-*.png
